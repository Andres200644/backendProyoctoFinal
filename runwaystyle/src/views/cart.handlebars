<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Carrito</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Carrito</h1>
    <ul>
        {{#each cart.products}}
            <li>
                {{this.name}} - {{this.price}}$
                <form action="/carts/{{../cart._id}}/products/{{this._id}}" method="post">
                    <button type="submit" formaction="/carts/{{../cart._id}}/products/{{this._id}}?_method=DELETE">Eliminar</button>
                </form>
            </li>
        {{/each}}
    </ul>
</body>
</html>
